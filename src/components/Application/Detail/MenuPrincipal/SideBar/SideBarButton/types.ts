export interface propsSideBarButton {
  text: string;
  icon: "Inbox" | "Time" | "Star" | "Sent" | "Paper";
  selected: boolean;
}

export interface propsSideBarButtonContainer {
  selected: boolean;
}
